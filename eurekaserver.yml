server:
  port: 8888
eureka:
  instance:
    hostname: localhost
  client:
    enabled: true
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/


